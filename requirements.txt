# Core runtime dependencies
numpy>=1.24.0

# NLP / ML — optional; only required when using the corresponding scorers
transformers>=4.35.0
evaluate>=0.4.0
datasets>=2.14.0
torch>=2.0.0
bert_score>=0.3.13
peft>=0.5.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
